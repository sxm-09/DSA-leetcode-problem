#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a;

    scanf("%d", &a);

    int arr[a];
  for (int i = 0; i < a; i++) {
        scanf("%d", &arr[i]);
    }

    int b;
   
    scanf("%d", &b);
int found = 0;
for (int i = 0; i < a; i++) {
        if (arr[i] == b) {
            found = 1;
            break;  // Exit loop early once found
        }
    }
 if (found) {
        printf("Found\n");
    } else {
        printf("Not Found\n");
    }

    return 0;
}

#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>

int main() {
    int a;
    scanf("%d",&a);
    int arr[a];
    for(int i=0;i<a;i++){
scanf("%d",&arr[i]);}
    int b;
    
    
    scanf("%d",&b);
    
    
    for(int i=0;i<a;i++){
   if(arr[i]==b){
   for(int j=i;j<a;j++){
  arr[j]=arr[j+1];}
       a--;break;}
    }
        for(int i=0;i<a;i++){
printf("%d ",arr[i]);}
    return 0;
}
